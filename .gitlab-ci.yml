include:
  - local: ci/binary.yml
  - local: ci/docker.yml

stages:
  - binary
  - docker

workflow:
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - when: never
